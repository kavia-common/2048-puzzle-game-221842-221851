# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.
#
# Lint goal for CI:
# - `dart format .` should produce no diffs
# - `flutter analyze` should report zero errors

# Use Flutter's recommended lints
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-raw-types: true
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  # Customize/extend rules from `flutter_lints`
  rules:
    # Style & safety
    prefer_final_locals: true
    prefer_final_fields: true
    unnecessary_const: true
    unnecessary_new: true
    always_use_package_imports: true
    prefer_relative_imports: false
    sort_pub_dependencies: true
    avoid_redundant_argument_values: true
    directives_ordering: true
    unnecessary_late: true
    avoid_unused_constructor_parameters: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    use_key_in_widget_constructors: true
    # Testing ergonomics
    avoid_print: true
    unnecessary_lambdas: true

# Additional information: https://dart.dev/guides/language/analysis-options
